# Screenshots

Acceptance engine path PASSED for CP2/CP4/CP5 (see ../summary.json).

Capture visual evidence on the operator machine:

1. desktop/Launch-SiteForge.bat
2. Import each Greensboro RUN (CP2, CP4, CP5) one at a time
3. After discovery (no Prefill): screenshot Transport, Sawtooth, Sorter, PLC Autogen
4. Save as cp2_transport.png, cp4_sawtooth.png, cp4_sorter.png, cp4_autogen.png, cp5_sorter.png, etc.

Automated Electron capture can be added via webContents.capturePage; engine-path L5X + JSON are the hard acceptance artifacts for this recovery commit.
